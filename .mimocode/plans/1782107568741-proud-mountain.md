# TradingBot GitHub Pages Website Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use compose:subagent or compose:execute to implement.

**Goal:** Create a simple GitHub Pages website for TradingBot, matching the design and deployment pattern from the finetuning project.

**Architecture:** Single self-contained HTML file served from `docs/` folder on `main` branch. Same Dracula theme, same fixed sidebar layout, same inline CSS + SVG icons approach. Zero dependencies. Same deployment as finetuning — GitHub Pages via repo Settings (legacy: branch `main`, folder `/docs`).

**Tech Stack:** HTML5, CSS3 (Dracula theme), inline SVG icons (Material-style), JetBrains Mono font via Google Fonts, GitHub Pages

---

## What to Create

### `docs/index.html`

A single self-contained HTML file (~550 lines) adapting the finetuning project's design to TradingBot content:

| Section | Content | Source |
|---------|---------|--------|
| Hero | Project name, tagline, description, GitHub + Quick Start buttons | README |
| Features | 4 cards: Transformer Model, Multi-Window Features, Portfolio-Aware Training, Sharpe Optimization | README |
| CLI Reference | Flag table (8 rows) with defaults + descriptions | README Usage |
| Model Architecture | Property table (d_model, params, layers, etc.) | README Model Details |
| Performance | Timing table (feature build, epoch, inference, etc.) | README Performance |
| How It Works | Numbered steps (Data → Features → Model → Training → Threshold) | README How It Works |
| Terminology | 6 terms: MSE/MSRR/Margin/ListNet, Mixed Precision, Decoder-Only, RankGLU, MarketGate, Walk-Forward, Z-Score | README Terminology |
| Quick Start | Code block with `uv sync` + `uv run python main.py` | README |
| Tech Stack | Badges: PyTorch, numpy, pandas, scikit-learn, yfinance | pyproject.toml |

Design details:
- Same Dracula color variables (`--bg`, `--panel`, `--fg`, `--muted`, `--purple`, `--pink`, `--cyan`, `--green`, `--orange`)
- Same JetBrains Mono font
- Same fixed sidebar (240px) with brand logo + nav links + 7 SVG icons
- Same mobile-responsive layout (collapsible sidebar at 900px)
- Same card grid, tables, code blocks, badges, and stats list styles
- Same sidebar footer with GitHub Pages credit
- Same OG meta tags (title, description, URL — no og-image for simplicity)
- Inline SVG favicon (trading chart icon instead of flag icon)

### No other files needed

- No `og-image.png` (user said "simple")
- No `_config.yml` (GitHub Pages Jekyll default passes through raw HTML)
- No `.nojekyll` (not needed — no Jekyll-conflicting files)

### Deploying

- GitHub Pages is already enabled via repo Settings: **Branch `main`, folder `/docs`**
- Pushing `docs/index.html` to `main` auto-deploys
- Site will be at `https://yuzu-octopus.github.io/Simple-Trader/`

---

## Files

- Create: `docs/index.html`

---

## Verification

1. Open `docs/index.html` in a browser — all sections render, sidebar nav links work, mobile toggle works
2. `ruff format --check .` stays clean (we're only adding an HTML file, not Python)
3. `uv run pytest` still passes (no Python changes)
4. Once pushed, verify at `https://yuzu-octopus.github.io/Simple-Trader/`
